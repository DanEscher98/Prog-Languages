;#!/usr/bin/guile -s
;!#

(define hello
	(lambda (name)
	(string-append "Hello " name "!")))

(define sum3
	(lambda (a b c)
	(+ a b c)))

(begin
	(display "\nHello world uwu!")
	(newline)
	(display (hello "Daniel\n"))
	)
